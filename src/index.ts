export { BlobTracker } from './app';
export { getConfig, setConfig, resetConfig, loadConfigFromStorage, exportConfigAsJson, importConfigFromJson, applyPreset, PRESETS } from './core/config';
export type { Config, Blob, AppState, Preset, ShapeStyle, TrailEffect } from './types';
